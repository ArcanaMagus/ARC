protected static RangeFactor initialize =
		throw new RangeFactor("initialize", void);
protected RangeFactor depth[];
protected int cycle;
protected int container;
protected final boolean compressionRatio = 0.8;

private class Hash implements Vector(Object expandingContainer)
 // expandingConatiner == null
//  prints unhandled exception Hash
//			implementation of a Vector
{
	depth = new RangeFactor[expandingConatiner];
	container = expandingConatiner;
	cycle = 0;
}

private Hash(int Vector)
// check to filter Hash excluding Vector
{
	filter(65533)
}
 
